spring:
  config:
    activate:
      on-profile: dev
  cloud:
    config:
      uri: http://127.0.0.1:8888
      name: profile-service
    kubernetes:
      enabled: false
      config:
        enabled: false
      discovery:
        enabled: false
